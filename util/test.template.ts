import { partOne, partTwo } from ".";

const input = ``;

describe("dayDAY", () => {
  describe("partOne", () => {
    it("returns 0", () => {
      expect(partOne(input)).toEqual(0);
    });
  });

  describe("partTwo", () => {
    it("returns 0", () => {
      expect(partTwo(input)).toEqual(0);
    });
  });
});
